# TODO

남은 작업과 결정 대기 항목. **현재 코드 상태는 `CLAUDE.md`에 적고, 앞으로 할 일만 여기에 둔다.**
끝난 항목은 지우고, 그 결과가 코드 동작을 설명하는 내용이면 `CLAUDE.md`로 옮긴다.

---

## 1. D v2를 새 회차로 다시 확인 (기한 없음)

겹침 특징을 넣은 뒤 297회차(946~1242) 검증에서 상관계수 0.674 / 기울기 1.004가 나왔다. 특징 8개 시절(296회차)의 0.639 / 1.05보다 낫지만 **같은 데이터를 한 번 더 잰 값이라 차이가 우연인지 가릴 수 없다.**

- 회차가 30~50회쯤 더 쌓이면 `/validation`을 다시 돌려 0.674가 유지되는지 본다.
- 크게 떨어지면 겹침 특징이 그 구간에만 맞았던 것이므로 되돌린다. 되돌릴 때 `MODEL_VERSION`도 함께 되돌려야 재학습된다.
- 설정을 바꿔 가며 여러 번 돌려 좋은 값을 고르면 그 값은 독립 평가가 아니다 (`PATTERN_EXPERIMENT.md` 마지막 문단).

## 2. 새 C·D 적중률 검증 (구현 끝, 검증 실행 대기)

C·D를 동반출현 게임(3개 + 랜덤 3 / 4개 + 랜덤 2)으로 바꿨다. 적중률 검증 표가 3게임에서 4게임이 되어 판정 기준도 0.0167 → 0.0125로 바뀌었다.

- `/validation`을 300회차 이상으로 돌려 C·D의 평균 일치 개수를 본다. **무작위(0.800)와 차이가 없는 것이 정상이다** — 많이 나온 조합은 우연이라는 것이 이 게임의 전제다.
- 결과를 `CLAUDE.md` 시간순 검증 문단과 `PATTERN_EXPERIMENT.md` 검증 기록 표에 적는다.
- 검증이 눈에 띄게 느려졌으면(회차마다 조합을 다시 셈) 루프 안에서 카운트를 증분 갱신하는 방식으로 바꾼다.

## 3. 문서 정리 (1·2번 끝난 뒤)

- `PopularityPredictor` javadoc에 남은 폐기 수치 교체: 클래스 주석의 상관계수 0.57 / 기울기 0.73 / "약 5% 이득", `CANDIDATE_POOL`의 합계 구간별 실측(1,041회차) → `CLAUDE.md`의 406회차 값 ("초기 200회차"는 가드 작업 때 정리함)
- `AGENTS.md`는 `CLAUDE.md`와 1·3행만 다르므로 함께 갱신

---

## 안 하기로 한 것

- **옛 회차(1~835) 동기화**: 위탁 운영 기관이 달라 인기도 계산의 신뢰도가 떨어진다. 다시 제안하지 말 것.
- **"4개 이상 등간격" 규칙**: 해당 회차가 6건뿐이라 t값이 커도 판단할 수 없다.
- **동반출현 조합 집계 테이블**: 407회차 기준 1.4만 번이면 다 세므로 저장할 이유가 없다. 테이블을 두면 ERD 관리와 이력 변경 시 무효화가 따라붙는다.
- **옛 회차를 공식 API로 받아 동반출현만 채우기**: 사이트 기본 화면(1~1242)과 번호를 맞추려던 것인데, 외부 의존이 생기고 시간순 검증에는 쓸 수 없다.
- **합계 상한**: 높은 합계에서도 효과가 꺾이지 않아 둘 이유가 없다.
- **E를 검증 대상에 넣기**: 무작위 기준은 이미 초기하분포 이론값을 쓴다. 시뮬레이션보다 이론값이 정확하다.
- **용지 패턴 특징(A)**: 적중에 도움이 없고 가장자리 번호로 쏠림만 만든다.
