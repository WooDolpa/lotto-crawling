# TODO

남은 작업과 결정 대기 항목. **현재 코드 상태는 `CLAUDE.md`에 적고, 앞으로 할 일만 여기에 둔다.**
끝난 항목은 지우고, 그 결과가 코드 동작을 설명하는 내용이면 `CLAUDE.md`로 옮긴다.

---

## 1. 옛 회차 학습 방지 가드 (해야 함)

836회(2018-12-08) 이전은 위탁 운영 기관이 달라 판매금액·당첨자 수를 지금과 같은 자로 잴 수 없다. 그래서 그 구간은 동기화하지 않기로 했다.

지금은 **코드가 이걸 막지 않는다.** `/system/sync?startNo=1`을 한 번 실행하면 D가 조용히 옛 데이터를 학습한다 (`TRAINING_WINDOW = 600`이 최근 600회를 잡으므로 836회 밑으로 내려간다). 오류도 경고도 없이 인기도 지수만 틀어진다.

- `LottoRules`에 `FIRST_TRUSTED_DRAW = 836` 추가 (2018-12-08, 동행복권 수탁 시작)
- `PopularityPredictor.train`이 이 회차 미만을 학습에서 제외
- A·B·C·E와 적중률 검증은 **건드리지 않는다.** 추첨된 공은 운영 기관과 무관하므로 전 회차를 써도 된다. 오염되는 것은 판매금액·당첨자 수를 쓰는 D뿐이다.

## 2. D에 "직전 회차 겹침" 특징 추가 (결정됨, 착수 전)

직전 회차 당첨 번호와 2개 이상 겹치는 조합은 **덜 붐빈다** (406회차 실측: 5등 −2.1% t=−5.0, 3등 −5.6% t=−5.2). 겹침 개수별로도 단조롭다 (0개 1.007 → 1개 0.996 → 2개 0.979).

지금 D는 직전 회차를 전혀 보지 않는다 (특징 8개가 전부 조합 자체의 생김새). 생성 시점에 직전 당첨 번호는 알고 있으므로 9번째 특징으로 넣을 수 있다. 가중치는 데이터가 정하게 두고, 효과는 검증 화면으로 확인한다.

| 파일 | 할 일 |
|---|---|
| `CombinationFeatures` | 특징 `last_draw_overlap`(0~6) 추가, 직전 회차 번호를 받도록 시그니처 변경 |
| `PopularityPredictor` | 학습·생성에 직전 회차 번호 전달 (학습은 창 시작 바로 앞 회차도 필요) |
| `LottoPopularityModelService` | `MODEL_VERSION`을 `popularity-v2-ridge9-fifth`로 (지문이 바뀌어 자동 재학습) |
| `LottoPredictionService` | 최신 회차 번호 넘기기 |
| `LottoValidationService` | 워크포워드 학습에 직전 회차 전달 |

**비교 기준** (현재 D, 296회차): 상관계수 0.639, 기울기 1.05, 하위 25% 0.9715.

## 3. C를 동반출현 게임으로 교체 (설계 확정 대기)

### 왜 바꾸나

지금 C(생일 조합 제외)는 규칙 자체는 맞지만 거르는 것이 전체의 9%뿐이라 기대 이득이 −0.35%다. 무작위(E)와 사실상 같아서 화면에 두 자리를 차지할 값어치가 없다.

### 먼저 알아야 할 사실

**"자주 나온 조합"은 전부 우연이다.** 1,241회차 실측에서 출현 횟수 분포가 공정한 추첨의 기대값과 거의 완전히 일치한다.

| 나온 횟수 | 3개 조합 (실제 / 우연) | 4개 조합 (실제 / 우연) |
|---|---|---|
| 0번 | 2,398 / 2,468 | 131,434 / 131,496 |
| 1번 | 4,365 / 4,317 | 16,545 / 16,429 |
| 2번 | 3,790 / 3,775 | 979 / 1,026 |
| 3번 | 2,253 / 2,201 | 36 / 43 |
| 4번 | 966 / 963 | **1** / 1 |
| 8번 | **6** / 5 | — |

**그리고 이 게임은 D와 반대 방향이다.** 동반출현 점수가 높은 조합일수록 실제로 더 붐볐다 (406회차: 3개 조합 기준 r=0.155 t=3.2, 짝 기준 r=0.183 t=3.7 — 점수 상위 25% 회차가 하위 25%보다 약 2% 더 붐빔). 통계 사이트가 "많이 나온 조합"을 알려주니 사람들이 실제로 그쪽을 산다.

당첨 확률은 어느 쪽이든 같다. 이 게임의 값어치는 **C(사람들이 좋아하는 쪽) ↔ D(사람들이 안 보는 쪽) ↔ E(기준)** 대비를 화면에서 보여 주는 데 있다. 그렇게 설명한다는 전제로만 만든다.

### 설계 3안 — 하나 고르면 착수

| | ① 3개 최다 + 랜덤 3개 | ② 4개 최다 + 랜덤 2개 | ③ 짝 점수로 4개 + 랜덤 2개 |
|---|---|---|---|
| 근거 표본 | 조합당 평균 1.75번 | 0.125번 | 짝당 18.8번 |
| 현재 1등 | 8번, **6개 동점** | 4번, `3-13-33-37` 하나 | `12-15-34-42` (점수 162) |
| 매주 번호가 바뀌나 | 동점 중 뽑으니 바뀜 | **안 바뀜 (영구 고정)** | 조금씩 바뀜 |
| 통계 비중 | 절반 | 2/3 | 2/3 |

③은 4개를 직접 세지 않고 그 안의 짝 6개 출현 횟수를 더해 점수를 낸다. 데이터를 가장 효율적으로 쓰면서 4개짜리 게임 형태를 유지한다.

### 공통 작업

- 새 `CoOccurrenceGameGenerator`, 화면 이름 `C · 동반출현 상위`
- `UnpopularGameGenerator` 삭제 (마지막 사용처가 사라짐)
- 당첨 번호만 쓰므로 **1,241회차 전부 사용** (운영 기관 무관)
- 화면 문구에 "붐비는 쪽" 성격을 명시

## 4. 문서 정리 (2·3번 끝난 뒤)

- `PopularityPredictor` javadoc에 남은 폐기 수치 교체: 상관계수 0.57 / 기울기 0.73 / "초기 200회차" / "합계 구간별 실측(1,041회차)" → `CLAUDE.md`의 406회차 값
- `PATTERN_EXPERIMENT.md`의 C 설명을 새 게임으로 교체
- `AGENTS.md`는 `CLAUDE.md`와 1·3행만 다르므로 함께 갱신

---

## 안 하기로 한 것

- **옛 회차(1~835) 동기화**: 위탁 운영 기관이 달라 인기도 계산의 신뢰도가 떨어진다. 다시 제안하지 말 것.
- **"4개 이상 등간격" 규칙**: 해당 회차가 6건뿐이라 t값이 커도 판단할 수 없다.
- **합계 상한**: 높은 합계에서도 효과가 꺾이지 않아 둘 이유가 없다.
- **E를 검증 대상에 넣기**: 무작위 기준은 이미 초기하분포 이론값을 쓴다. 시뮬레이션보다 이론값이 정확하다.
- **용지 패턴 특징(A)**: 적중에 도움이 없고 가장자리 번호로 쏠림만 만든다.
