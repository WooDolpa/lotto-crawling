# 예측 모델 저장·갱신과 시간순 검증

> 파일·클래스 이름의 "pattern"은 이전 "패턴 모델"에서 유래했다. 용지 패턴 특징은 검증 결과 적중에 도움이 없고 번호 쏠림만 만들어 제거했으며, 현재 A는 기본 6개 특징만 쓰는 점수 모델이다.

## 저장 모델과 매주 갱신

- 다음 회차 예측은 `GET /api/lotto/prediction`. DB 전체 이력으로 학습한 A 점수 모델에서 상위 6개를 반환한다(B 확률 모델, C·D 동반출현 게임, E 인기도 모델과 함께). 예측 요청은 학습하지 않는다.
- 시작 시 `models/lotto-pattern.bin`을 로드하고 백그라운드에서 DB 변경을 확인한다. 모델이 없거나 데이터가 달라졌다면 최초/갱신 학습한다. 동일 데이터는 재사용한다.
- API 동기화 이벤트는 트랜잭션 커밋 후 처리한다. 롤백이면 학습하지 않는다. 별도로 1시간마다(`lotto.model.check-interval-ms`) DB를 확인해 직접 DB에 추가한 회차도 감지한다.
- 데이터 해시(`HistoryFingerprint`)는 회차·당첨 번호와 모델 설정 버전을 포함한다. 기존 회차 수정도 재학습하며, A·B는 날짜·당첨금만 바뀌면 재학습하지 않는다. E는 인기도 계산에 쓰는 총 판매금액·5등 당첨자 수까지 해시에 넣으므로(`ofPopularity`) 그 값만 채워져도 다시 학습한다.
- 학습은 단일 백그라운드 작업으로 순차 처리한다. 학습 중 적재 이벤트가 발생하면 완료 후 다시 확인한다. 기존 모델은 계속 사용 가능하다.
- 새 모델의 학습, 추론 확인, 임시 파일 저장, 원자적 파일 교체가 모두 성공한 후 메모리 모델을 교체한다. 실패 시 기존 모델과 파일을 유지하고 로그/상태에 오류를 표시한다. 변경 데이터는 다음 점검 때 재시도한다.
- 모델 저장 위치는 `LOTTO_PATTERN_MODEL_FILE`로 변경할 수 있다. 기본 위치는 실행 디렉터리의 `models/lotto-pattern.bin`. 쓰기 권한 및 원자적 교체를 지원하는 로컬 파일시스템이 필요하다. 모델 파일은 이 애플리케이션이 생성한 파일만 사용한다.
- `GET /api/lotto/prediction/status`: 학습 중 여부, 모델별 가용 여부·학습 기준·최근 오류.
- `POST /api/lotto/prediction/train`: 변경 확인을 요청(202). 데이터가 동일하면 다시 학습하지 않는다.
- 최소 50건의 연속된 유효 이력 필요. 새 이력이 학습되기 전 예측은 최신 특징을 이전 모델에 입력하며 `staleModel=true`로 명시한다.
- 시간순 검증은 별도 기능이며 실행할 때마다 따로 학습한다.

## 시간순 검증

모델 검증 화면(`/validation`)에서 실행한다. 운영 모델과 별개로 실행할 때마다 따로 학습한다.

`POST /api/lotto/validation?testDraws=300` (시작, 202) / `GET /api/lotto/validation` (상태·결과)

- DB 전체 이력(연속·유효해야 함)에서 최근 testDraws(20~500)개 회차를 검증한다. 최소 이력은 50 + testDraws.
- 각 검증 회차는 그 회차보다 이전 이력만 사용한다. 특징은 회차마다 한 번 계산해 모든 구간·모델이 공유한다.
- 20회차마다 그 이전 이력 전체로 다시 학습하고, 구간 안에서는 같은 모델에 최신 특징을 넣는다(운영 모델이 재학습 전 쓰는 방식과 같음).
- 비교 게임
  - A 점수 모델: 기본 6개 특징, 60 trees·maxDepth 12·maxNodes 100·nodeSize 5, seed 42, 점수 상위 6개.
  - B 확률 모델: v1과 같은 Smile 기본 설정 + 트리 시드 42 + 확률 가중 샘플링·밸런스 필터로 1게임(뽑기 시드 42 + 회차).
  - C 동반출현 3개: 그 회차 이전 이력에서 가장 자주 함께 나온 3개(동점이면 그중 무작위) + 무작위 3개(뽑기 시드 42 + 회차).
  - D 동반출현 4개: 같은 방식으로 4개 + 무작위 2개.
- E 인기도 모델은 적중률이 아니라 인기도를 노리므로 위 네 게임과 같은 표에 넣지 않고 따로 잰다.
  - 20회차마다 그 이전 이력으로 E를 학습하고, 검증 회차의 **실제 당첨 조합**의 인기도를 예측해 실제 인기도와 비교한다. 생성한 조합은 당첨되지 않아 실제 인기도를 알 수 없으므로 이 방법을 쓴다.
  - 예측-실제 상관계수, 보정 기울기(실제 = a + b × 예측), 예측 하위 25%·상위 25% 회차의 실제 평균 인기도를 낸다.
  - 판정은 상관계수 0 가정의 단측 t 검정(자유도 n−2) p값이 0.05보다 작은지로 한다. 가설이 하나라 게임 수로 나누지 않는다.
  - `LottoRules.FIRST_TRUSTED_DRAW`(836회) 미만과 판매금액·5등 당첨자 수가 없는 회차는 학습·채점 모두에서 건너뛴다. 남은 회차가 20건 미만이면 이 항목만 건너뛴다.
- 무작위 기준은 이론값이다. 미리 정한 6개가 당첨 번호와 k개 일치할 확률은 C(6,k)·C(39,6−k)/C(45,6)로, 평균 0.8개, 3개 이상 약 2.38%.
- 판정
  - 평균 일치 개수: 회차별 일치 개수 분포를 testDraws번 합성한 정확한 분포에서 "합계가 관측값 이상일 확률"(단측 p값).
  - 3개 이상 일치 비율: 이항분포 단측 p값(참고용).
  - 4개 게임을 함께 판정하므로 p값이 0.05 ÷ 4 = 0.0125보다 작을 때만 "무작위보다 나음".
- 한 번에 한 건만 실행(실행 중 요청은 409). 상태는 서버 메모리에 마지막 1건만 보관하며 재시작하면 사라진다.

같은 기간으로 설정을 바꿔 가며 여러 번 검증해 좋은 결과를 고르면 그 결과는 독립 평가가 아니다. 설정을 고정한 뒤 이후 새로 쌓인 회차로 다시 확인해야 한다.

## 검증 기록 (406회차 데이터, 356회차 검증: 886~1241회)

| 모델 구성 | 평균 일치 (무작위 0.8) | 우연일 확률 | 비고 |
|---|---|---|---|
| 패턴 v1 (기본 6 + 위치·모양 252) | 0.789 | 61% | 맨 오른쪽 줄 번호 비율 26.5% (실제 14.1%), 43·42번 쏠림 |
| 패턴 v2 (기본 6 + 용지 위 관계 50) | 0.716 | 98% | 쏠림 29.8%로 더 심해짐 |
| 기본 6개만 (A′, 현재 A 구성) | 0.772~0.781 | 69~76% | 쏠림 없음 (11.5~13.9%) |
| B 확률 모델 | 0.815~0.823 | 30~37% | 3개 이상 일치 4.2%(판정 기준 미달) |
| 옛 C (생일 조합 제외, 삭제됨) | 0.759~0.789 | 61~85% | 당첨 확률 개선 목적 아님 |
| C 동반출현 3개 (최다 3개 + 무작위 3) | 0.807 | 45% | 357회차 검증(886~1242회). 3개 이상 일치 2.5% |
| D 동반출현 4개 (최다 4개 + 무작위 2) | 0.801 | 50% | 357회차 검증(886~1242회). 3개 이상 일치 2.0%, 4개 이상 0회 |

모든 구성이 무작위와 차이가 없었다. 같은 설정으로 두 번 실행한 결과는 회차별 번호까지 동일했다(시드 고정 확인).

C·D를 잰 357회차 실행(407회차 데이터)에서 A는 0.782, B는 0.824(3개 이상 일치 4.2%)로 앞선 기록과 같은 수준이었다. 평균 일치 개수의 표준오차는 약 ±0.042라 네 게임 모두 그 안에 있다. 당첨 번호에 자주 함께 나온 조합을 고정해도 적중률이 오르지 않는다는 C·D의 전제가 확인됐다. 실행 시간은 15초로, 회차마다 조합을 다시 세는 방식이 느리지 않았다.
