# 로또 용지 패턴 실험

## 저장 모델과 매주 갱신

- 다음 회차 예측은 `GET /api/lotto/pattern/predict`. DB 전체 이력(405건이면 모두 사용)으로 학습한 저장 모델에서 상위 6개를 반환한다. 예측 요청은 학습하지 않는다.
- 시작 시 `models/lotto-pattern.bin`을 로드하고 백그라운드에서 DB 변경을 확인한다. 모델이 없거나 데이터가 달라졌다면 최초/갱신 학습한다. 동일 데이터는 재사용한다.
- API 동기화/엑셀 적재 이벤트는 트랜잭션 커밋 후 처리한다. 롤백이면 학습하지 않는다. 별도로 60초마다 DB를 확인해 직접 DB에 추가한 회차도 감지한다.
- 데이터 해시는 회차·정렬된 당첨 번호·특징 및 모델 설정 버전을 포함한다. 기존 회차 수정도 재학습하며 날짜/당첨금만 바뀌면 재학습하지 않는다.
- 학습은 단일 백그라운드 작업으로 순차 처리한다. 학습 중 적재 이벤트가 발생하면 완료 후 다시 확인한다. 기존 모델은 계속 사용 가능하다.
- 새 모델의 학습, 추론 확인, 임시 파일 저장, 원자적 파일 교체가 모두 성공한 후 메모리 모델을 교체한다. 실패 시 기존 모델과 파일을 유지하고 로그/상태에 오류를 표시한다. 변경 데이터는 다음 점검 때 재시도한다.
- 모델 저장 위치는 `LOTTO_PATTERN_MODEL_FILE`로 변경할 수 있다. 기본 위치는 실행 디렉터리의 `models/lotto-pattern.bin`. 쓰기 권한 및 원자적 교체를 지원하는 로컬 파일시스템이 필요하다. 모델 파일은 이 애플리케이션이 생성한 파일만 사용한다.
- `GET /api/lotto/pattern/status`: 모델 가용 여부, 학습 중 여부, 학습 기준, 최근 오류.
- `POST /api/lotto/pattern/train`: 변경 확인을 요청(202). 데이터가 동일하면 다시 학습하지 않는다.
- 최소 50건의 연속된 유효 이력 필요. 새 이력이 학습되기 전 예측은 최신 특징을 이전 모델에 입력하며 `staleModel=true`로 명시한다.
- 기존 실험 API는 별도 성능 검증 기능이며 실행 시 재학습한다. 최근 300건 제한은 실험에만 적용된다.

홈 화면의 `패턴 학습 실험`에서 명시적으로 실행한다. 기존 회차 검색과 독립적이며 추천 API는 변경하지 않는다.

`POST /api/lotto/pattern/experiment?testDraws=5&seed=42`

- 최근 최대 300건의 연속 회차 사용. 최소 이력은 50 + 검증 회차 수. 검증 수 1~20.
- 각 검증 회차보다 이전 데이터만 학습하는 expanding-window 검증. 마지막에 전체 이력으로 다음 회차 후보 생성.
- 비교 모델: 기존 번호 특징 6개 / 번호 특징 + 번호 좌표 + 최근 10회차의 패턴 특징.
- 패턴 특징: 중심, 범위, 선 길이, 꺾임 각도 합, 행/열 분포, 후보 번호와 중심의 차이, 같은 행/열의 점 수, 최근 패턴 점까지 최소 거리.
- 두 모델 모두 Smile RandomForest 60 trees, maxDepth 12, maxNodes 100, nodeSize 5. 트리별 seed 스트림 고정.
- 점수 상위 6개를 사용하며 동점은 번호 오름차순. 기존 추천 API의 가중 샘플링과 필터는 비교에 사용하지 않음.
- 무작위 비교는 회차별 seed로 1,000개 균등 비복원 조합의 평균. 무작위 평균 일치의 이론값은 6×6/45 = 0.8.
- 모양 유사도: 정렬된 점끼리 평균 유클리드 거리를 격자 대각선 길이로 나누고 1에서 뺌. 같은 패턴 1, 멀수록 감소. 화면 격자의 간격 28×33 사용.
- 모델 오류를 균등 확률로 대체하지 않음. HTTP 오류로 실험 실패 표시.

모양은 용지 배치에 의존한다. 오름차순 연결은 실제 추첨 순서가 아니다. 적은 검증 회차에서 우세한 결과는 예측력의 증거가 아니며, 반복적으로 설정을 조정한 검증 결과도 독립 평가로 볼 수 없다. 향후 설정을 고정한 뒤 별도 미사용 회차로 평가해야 한다.

검증 실행: `gradlew patternExperimentCheck` (DB 없이 합성 이력 사용).

저장/갱신 검증: `gradlew patternModelCheck`. 합성 이력 405건 전체 학습, 파일 재시작 복원, 예측 시 무학습, 신규/수정 데이터 감지, 실패 시 기존 모델 유지, 커밋/롤백 이벤트를 확인한다. 실제 DB의 성능 검증과는 별개다.
